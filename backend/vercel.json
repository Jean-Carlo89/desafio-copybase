{
    "version": 2,
    "builds": [
        {
            "src": "./src/express/app.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/src/express"
        }
    ]
}